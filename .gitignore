# Local per-deployment config. NEVER commit — contains OIDC_CLIENT_SECRET.
deploy.env

# PKI material generated by vpn-setup.sh. All *.key files are private keys.
*.key
*.crt
*.csr
*.srl
*.ovpn

# Per-user Claude Code state (contains local paths + per-user preferences).
# .claude/settings.json is shared/committed; .local.json is per-user.
.claude/settings.local.json

# Editor / OS scratch
.DS_Store
*.swp
