# --- Secrets / PKI: NEVER commit ---
*.key
*.crt
*.csr
*.srl
*.pem
*.ovpn
ca.*
server.*
client.*
cli.cnf
srv.cnf

# --- Local env / rendered config: NEVER commit (your account + infra values) ---
deploy.env
deploy.env.local
gateway.yaml
*.rendered.yaml

# --- Local / IDE / OS ---
.claude/
.vscode/
.DS_Store

# --- stray/generated ---
listening
*.rendered.yaml
